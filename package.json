{
  "name": "picoserve",
  "version": "1.0.0",
  "description": "TypeScript-based Node.js and Express.js server for serving static files",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env dist/server.js",
    "dev": "node --env-file=.env -r ts-node/register --watch src/server.ts",
    "lint": "oxlint src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "static-files",
    "server",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.8.1",
    "oxlint": "^1.23.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
